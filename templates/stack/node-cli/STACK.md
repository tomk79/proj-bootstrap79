- 依存は最小に。標準モジュールで済むなら足さない（`npx` で落ちてくる量とサプライチェーンの面積が減る）。
- `bin/` に入口、`lib/` にロジック。入口は引数の解釈だけにして、ロジックはテストできる形に分ける。
- Lint / Format: `eslint` + `prettier`。テスト: `node --test`（標準）で足りなければ `vitest`。
- 対話プロンプトは `node:readline/promises` で足りる。
