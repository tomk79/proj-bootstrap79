# proj-bootstrap79: AGENTS.md

この文書は、AI エージェントとコントリビュータが**このリポジトリ自体**で作業するときの前提とルール。
`templates/base/AGENTS.md` は生成物（新しいプロジェクトに置かれる置き手紙）のテンプレートであって、この文書ではない。混同しないこと。

## 0. このリポジトリは何か

まっさらなリポジトリに、アプリを書き始める**前**に置くものを置く CLI。`npx github:tomk79/proj-bootstrap79` で走る。

質問（名前・アプリ種別・スタック・CI・ホスティング）に答えさせ、`templates/` を現在のディレクトリへ展開し、最後に「次に叩く公式コマンド」を表示する。**フレームワークの雛形は生成しない**（`npm create tauri-app` 等に任せる。複製は腐るため）。

利用者向けの説明は [README.md](README.md)。設計の前提もそこに書いてある。README とこの文書がずれたら README を正とする（利用者が読むのはそちら）。

## 1. 構成

| パス | 役割 |
|---|---|
| `bin/proj-bootstrap79.js` | 入口。対話・フラグ解釈・テンプレート展開・次手順の表示まで全部。現状ここ1ファイル |
| `lib/questions.mjs` | 質問の選択肢テーブル（`APP_TYPES` / `STACKS` / `CI` / `HOSTING`）。データだけを持つ |
| `templates/base/` | 種別によらず必ず置くもの。ツリーごと対象ディレクトリへコピーされる |
| `templates/stack/<key>/STACK.md` | スタックごとの作法。**コピーされず**、`{{STACK_SECTION}}` として `AGENTS.md` に埋め込まれる |
| `templates/stack/<key>/files/` | そのスタックのときだけ置くファイル（任意。現状どのスタックにも無い）。あればツリーごとコピーされる |
| `try/` | ローカル動作確認用の捨て場。git 管理外 |

## 2. 変えてはいけない制約

- **依存ゼロを保つ。** `package.json` に `dependencies` / `devDependencies` を足さない。
  - 由来: `npx` で毎回落ちてくる量と、サプライチェーンの面積を最小にするための設計。対話は `node:readline` で足りている。
- **Node 20+ の標準モジュールだけで書く。** ESM（`"type": "module"`）。
- **既存ファイルは上書きしない。** `--force` が付いたときだけ上書きする（`bin/proj-bootstrap79.js` の `copyTree`）。この挙動を壊さない。
  - 由来: 既に走り始めたリポジトリに対して二度目を叩いても壊れないことが、この道具の前提になっている。
- **`templates/base/gitignore` の名前にドットを付けない。** `bin` の `RENAME` が展開時に `.gitignore` へ戻す。
  - 由来: `npm pack` は `.gitignore` をパッケージから落とす。ドット付きで置くと `npx github:` 経由のときだけ静かに `.gitignore` が生成されない（実測で確認）。
- **総量を小さく保つ。** テンプレートに何かを足すときは、何を削るかを一緒に考える。置き手紙は読まれなければ意味がない。
- **日本語で書く。** コード内コメント・テンプレート・README・この文書すべて。

## 3. 変えるときの手順

### スタックを1つ足す

1. `lib/questions.mjs` の `STACKS` にキーを足す。`label` / `for`（対応するアプリ種別の配列）/ `scaffold`（公式スキャフォルダのコマンド）の3つが要る。
2. `templates/stack/<キー>/STACK.md` を作る。**キーはディレクトリ名と一致させる**（`bin` はこの一致を前提に読む）。
   - 中身は箇条書きのみ。見出しは付けない（`AGENTS.md` の `### スタックの作法` の下に素で差し込まれる）。
   - 書くのは「そのスタックで踏む固有の落とし穴」と「lint / format / test に何を使うか」。一般論は `templates/base/AGENTS.md` の担当。
3. そのスタックだけ置きたいファイルがあれば `templates/stack/<キー>/files/` に。無ければ作らない。
4. `try/` で実際に流して、`AGENTS.md` に埋まることを目で見る（→ 4節）。

STACK.md を置かないままスタックを足すと `（このスタックの作法はまだ整備されていない）` が生成物に出る。落ちはしないが、それは未完成の合図。

### アプリ種別を足す

`lib/questions.mjs` の `APP_TYPES` にキーを足したら、**併せて必ず**:

- `STACKS` に `for` でその種別を含むものを最低1つ用意する。
- `HOSTING.none.for` にその種別を足す。

どちらかを忘れると、その種別を単独で選んだとき候補が空になり、`choose` が答えを受け付けられず抜けられなくなる。

### 質問そのものを足す

`lib/questions.mjs` にテーブルを足し、`bin` 側で `choose` / `chooseMany` を呼ぶ。フラグ名（`--xxx=`）はテーブル名に合わせる。フラグは対話を飛ばす経路であり、**AI や CI から呼ぶときの唯一の入口**なので、対話でできることはフラグでも必ずできる状態を保つ。

### テンプレートの変数

`render()` が `{{NAME}}` を置換する。展開されるファイル**すべて**が対象（`.json` も含む）ので、テンプレート内にリテラルの `{{...}}` を書かない。

現在渡している変数: `APP_NAME` `APP_TYPE` `STACK` `CI` `HOSTING` `STACK_SECTION` `SCAFFOLD` `DATE`。
このうち `SCAFFOLD` はどのテンプレートからも参照されていない（次手順の表示は `scaffoldCmd()` を直接使う）。使うか消すかを決めるまで、これは未使用のまま。

未定義の変数は置換されず `{{NAME}}` のまま生成物に残る。タイプミスは静かに通るので、追加したら 4節 で実物を見ること。

## 4. 動作確認

テストは無い。**変更したら必ず `try/` で実際に走らせて、生成物を目で見る。** `try/` は `.gitignore` 済み。

```
mkdir -p try/sample && cd try/sample
node ../../bin/proj-bootstrap79.js
```

対話を飛ばす（AI から確認するときはこちら）:

```
node ../../bin/proj-bootstrap79.js --name=sample-app --type=cli --stack=node-cli --ci=github --hosting=none
```

複数種別（`apps/<種別>/` に分かれる経路。ここが一番壊れやすい）:

```
node ../../bin/proj-bootstrap79.js --name=sample-app --type=desktop,web --stack=tauri,next-vercel-supabase --ci=github --hosting=vercel
```

見るべきは、コマンドの終了コードではなく**置かれたファイルの中身**:

- `AGENTS.md` の表と `### スタックの作法` が埋まっているか（`{{...}}` が残っていないか）
- 複数種別のとき、種別ごとの見出しと置き場（`apps/<種別>/`）が出ているか
- 同じディレクトリで二度叩いて「既にあるので触らなかったもの」に落ちるか。`--force` で上書きされるか
- `.gitignore` が置かれているか（`templates/base/gitignore` からのリネーム。ここは落ちやすい）

確かめ終わったら捨てる: `cd ../.. && rm -rf try/sample`

## 5. 作業ルール

1. **変更はタスクに必要な範囲に留める。** 無関係なリファクタ・未依頼のドキュメント追加はしない。
2. **成功判定は生成物の実測で取る。** このリポジトリの成果物は「他所のディレクトリに置かれたファイル」なので、`bin` が例外なく終わったことは何の保証にもならない。4節を通す。
3. **`templates/` を触ったら README の表とずれていないか見る。** 置くものの一覧は README にもある。
4. **書いた AI に検品させない。** レビューは別セッションで。
5. **`try/` の中身をコミットしない。**
6. **推測には出典と確度を添える。** 実測した範囲だけを「確認した」と書く。

## 6. コマンド

このリポジトリ自体には依存もビルドもテストもない。

- 動作確認: 4節
- 公開: `npx github:tomk79/proj-bootstrap79`（レジストリには出していない。`package.json` の `files` に `bin` `lib` `templates` が入っていることだけ保つ）
